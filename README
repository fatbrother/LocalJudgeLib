clone the package
moves judgeLib package to where you want to use it
import judgeLib
with this package 
you should have psutil, use pip install requirements.txt
see the sample.py file to have a view of the package